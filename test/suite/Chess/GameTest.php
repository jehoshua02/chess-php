<?php

use \Chess\Game;
use \Chess\Piece;

class GameTest extends PHPUnit_Framework_TestCase
{
    public function testWhiteMovesFirst()
    {
        // $game = new Game();
        // $this->assertTrue($game->move('D2', 'D3'), 'Light should be able to move first');

        // $game = new Game();
        // $this->assertFalse($game->move('D7', 'D6', 'Dark should not be able to move first'));
    }
}